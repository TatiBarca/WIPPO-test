import React from "react";
import s from './Connection.module.css';

export const Connection = () => {
    return (
        <div>
            <div className={s.connection}>
            <div className={s.titleBlock}>
                <h1>Ready for your project</h1>
                <button>Get in touch</button>
            </div>
            </div>
        </div>
    )
}